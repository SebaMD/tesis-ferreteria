// Add your optimized WebP/AVIF to frontend/public/images/, then set its URL here
// (for example /images/catalog-background.webp). Leave null until the asset exists.
// This background is applied once to the catalog section, never to ProductCard.
export const CATALOG_BACKGROUND_IMAGE = null;
